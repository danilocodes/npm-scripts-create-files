### Using NPM scripts to create components following templates

Usage:

```bash
npm run create:component --name=<component name>
```

Executing:

```bash
npm install
npm run dev
```

See `package.json` for details on how the script works